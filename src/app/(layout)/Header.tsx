import Image from 'next/image';
import Link from 'next/link';
import HeaderNav from './HeaderNav';

const Header = () => {
  return (
    <header className=''>
      <Link href={} className='relative h-[75px] w-[100px]'></Link>

      <HeaderNav />
    </header>
  );
};

export default Header;
